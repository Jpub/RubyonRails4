<%= @user.username %>님

사이트에 사용자 등록해주셔서 감사드립니다.
다음은 등록해주신 정보입니다.

사용자 이름: <%= @user.username %>
비밀번호: (보안을 위해 보이지 않습니다.)
메일 주소: <%= @user.email %>

사이트 이용을 위해 다음 페이지에서 로그인해주세요.

<%= url_for(host: 'www.example.com', controller: :books,
  action: :index) %>

<%#= url_for(controller: :books, action: :index,
  only_path: false) %>

서버 사이드 기술 배움터(webmaster@wings.msn.to)